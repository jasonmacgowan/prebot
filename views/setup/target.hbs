<h1>Connecting to GitHub</h1>

<div class="text-left">
  {{#if errors}}
  <div class="flash mt-3 flash-error">
    {{#each errors}}
    <p>{{this}}</p>
    {{/each}}
  </div>
  {{/if}}
  <p>Set the GitHub URL this app should connect to.</p>
  <form method="POST">
    <div class="form-group">
      <div class="form-group-header">
        <label for="GHE_URL">GitHub Url (leave blank for GitHub.com)</label>
      </div>
      <div class="form-group-body">
        <input class="form-control" name="GHE_URL" type="text" placeholder="https://github.com/" value="{{ url }}" />
      </div>
    </div>

    <div class="form-actions">
      <button type="submit" class="btn btn-primary">Next</button>
    </div>
  </form>
</div>